spring:
  application:
    name: cloud-config-server
  cloud:
    consul:
      host: localhost
      port: 8500
    config:
      server:
        git:
          uri: file://${user.home}/config-repo
  security:
    user:
      name: user
      password: 1234
  zipkin:
    base-url: http://localhost:9411

encrypt:
  key: 1234567890
